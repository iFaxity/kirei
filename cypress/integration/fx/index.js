import { toRef, toRefs, toRawValue, isRef, ref, reactive } from '@kirei/fx';
import { strict as assert } from 'assert';

describe('@kirei/fx', () => {
  describe('toRef', () => {
  });

  describe('toRefs', () => {
  });

  describe('toRawValue', () => {
  });
})
