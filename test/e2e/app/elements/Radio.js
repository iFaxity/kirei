import { defineElement, html, ref, reactive } from '@kirei/element';

export default defineElement({
  name: 'AppRadio',
  props: {
    
  },
  setup(props) {
    return () => html`
    
    `;
  },
});
